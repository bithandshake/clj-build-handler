
(ns build-handler.api
    (:require [build-handler.utils :as utils]))

;; ----------------------------------------------------------------------------
;; ----------------------------------------------------------------------------

; @redirect (build-handler.utils/*)
(def filepath<-build-version utils/filepath<-build-version)
(def uri<-build-version      utils/uri<-build-version)
